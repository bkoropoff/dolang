# UserFlags

Native Windows user-account flags returned by [`UserInfo`](./user-info.md).

Unknown native bits are preserved.

## Fields

### `int`

Returns the complete native mask as an integer, including unknown bits.

#### Returns

[`Int`](../std/int.md)
